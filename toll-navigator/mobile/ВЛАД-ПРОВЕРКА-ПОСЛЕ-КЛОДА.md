# ✅ ПРОВЕРКА ПОСЛЕ КЛОДА

Клод отработал — теперь нужно проверить, что он не наделал глупостей.

## Как проверить:

**1. Открой терминал и запусти:**
```bash
cd "/Users/vladimirprihodko/Папка тест/fixcraftvp/toll-navigator/mobile"
```

**2. Проверь код (ошибок нет или нет):**
```bash
npx eslint screens/*.js
```
Если вывод пустой или "0 errors" — ок.

**3. Проверь версию:**
```bash
grep versionCode app.json
```
Должно быть новое число (больше старого).

**4. Проверь лог Клода:**
```bash
tail -20 /tmp/claude_v13.log
```
Должно быть "Build success" или "Upload succeeded".

**5. Проверь, что файл приложения скачался:**
```bash
ls -lh /tmp/haulwallet-*.aab
```
Если файл есть — ок.

**6. Проверь базу (backend):**
```bash
curl -s http://localhost:3001/api/health
```
Должно вывести: `{"status":"ok"}`

---

## Если всё ок:
→ Скопируй путь к AAB файлу и отправь в Telegram Hermes'у для загрузки в Google Play.

## Если что-то не так:
→ Отправляй лог Hermes'у — он разберётся.
