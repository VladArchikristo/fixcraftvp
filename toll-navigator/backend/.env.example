PORT=3000
JWT_SECRET=change_this_to_a_long_random_string_at_least_32_chars
DB_PATH=../data/toll_navigator.db

# Public base URL used to build tracking links sent to brokers.
# Set to your server's public IP or domain (no trailing slash).
# Example: http://192.168.1.100:3000  or  https://api.yourapp.com
BASE_URL=http://localhost:3000
